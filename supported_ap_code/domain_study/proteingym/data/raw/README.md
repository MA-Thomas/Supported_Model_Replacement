# Pinned ProteinGym inputs

The raw archives are intentionally not committed. Download and checksum-verify
the pinned inputs with:

```sh
sh domain_study/proteingym/run.sh download
```

The study uses ProteinGym v1.3 substitution data and zero-shot model scores,
plus assay metadata pinned at upstream commit
`144fe22b07dfaeec2b366f2346203a9838a55b4c`.
