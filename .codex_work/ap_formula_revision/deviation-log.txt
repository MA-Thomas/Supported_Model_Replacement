Output slide 3 (source slide 3):
- Rewrites the title, the two card labels and descriptions, and the bottom callout.
- Preserves both colored cards, both existing precision/recall definition images, all geometry, and all typography.
- Updates the inherited speaker notes to explain the P_n and recall-increment connection.

Output slide 4 (source slide 35):
- Recasts the backup threshold-sum slide as a “WHY AP?” bridge slide.
- Replaces four inherited equation images with exact AP formula artwork.
- Rewrites only the inherited title, section labels, slide number, and three definition rows.
- Preserves all borders, frames, positions, typography, and background.
- Updates the inherited speaker notes to match the new AP-formula teaching role.
- Sets the two relabeled section-tag text insets to zero vertically so the imported 14–15 pt labels remain visible inside their inherited 20 px frames.

Output slides 5–41:
- Preserve the original slides 4–40 and alter only the visible two-digit slide number to reflect the insertion.
